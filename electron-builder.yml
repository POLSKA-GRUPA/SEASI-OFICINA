appId: com.seasi.oficina
productName: La Oficina
directories:
  output: dist
  buildResources: build
files:
  - out/**
  - package.json
mac:
  category: public.app-category.business
  target:
    - target: dmg
      arch:
        - arm64
  # Fase comercial: identidad + hardenedRuntime + entitlements + notarización
  # identity: null mientras dure la fase interna (builds ad-hoc para PGK)
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
dmg:
  title: "La Oficina ${version}"
  artifactName: "La-Oficina-${version}-arm64.dmg"
win:
  target:
    - target: nsis
      arch:
        - x64
  # Fase comercial: azureSigningOptions (Trusted Signing)
nsis:
  oneClick: false
  perMachine: false
  artifactName: "La-Oficina-Setup-${version}-x64.exe"
publish: null
