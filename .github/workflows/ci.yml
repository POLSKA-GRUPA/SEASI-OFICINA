name: ci

on: [push]

jobs:
  quality:
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          path: seasi-oficina
      - uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/SEASI-CORE
          path: SEASI-CORE
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: astral-sh/setup-uv@v6
      - name: Preparar kernel (uv sync del venv real)
        run: uv run --project SEASI-CORE python -c "import seasi_core"
        shell: bash
      - name: Generar contratos + gate de digests + auditoría IPC
        working-directory: seasi-oficina
        run: |
          npm install --no-audit --no-fund
          npm run contracts:check
          npm run audit:ipc
          npm run typecheck
          npm test
        env:
          SEASI_SCHEMAS_DIR: ${{ github.workspace }}/SEASI-CORE/schemas/v1
          ELECTRON_SKIP_BINARY_DOWNLOAD: "1"
        shell: bash
